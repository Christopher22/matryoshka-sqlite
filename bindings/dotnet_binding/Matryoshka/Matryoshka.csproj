<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>netcoreapp3.1</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <!-- We need to ensure that there is no clash of shared libraries during testing -->
    <AssemblyName>MatryoshkaManaged</AssemblyName>
  </PropertyGroup>

</Project>
